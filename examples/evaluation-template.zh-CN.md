# RAG 评估模板

这份模板用于在开发过程中同时检查检索质量与回答质量。

## 测试问题

**用户问题**

```text
[问题]
```

**预期信息需求**

```text
[一个有用的回答应该覆盖哪些内容]
```

## 检索结果检查

**检索到的知识片段**

```text
1. [来源 / chunk 摘要]
2. [来源 / chunk 摘要]
3. [来源 / chunk 摘要]
```

**检索质量**

- 是否检索到相关上下文：是 / 部分 / 否
- 是否遗漏重要信息：是 / 否
- 是否包含无关上下文：是 / 否
- 是否存在重复或高度相似的召回：是 / 否

**说明**

```text
[为什么这次检索成功或失败]
```

## 回答质量检查

**模型生成回答**

```text
[回答]
```

**评估**

- 是否与检索上下文一致：是 / 部分 / 否
- 是否覆盖主要问题：是 / 部分 / 否
- 是否引入无依据的信息：是 / 否
- 是否需要继续调整检索或 Prompt：是 / 否

**下一步要测试的改动**

```text
[chunk 切分 / 检索 / 上下文 / Prompt 调整]
```
